# Copy this file to .env.local (dev) — Vercel project settings handle
# production/preview. Never commit real .env.local/.env.production files.

# Backend API base URL. Defaults to http://localhost:8000 if unset.
NEXT_PUBLIC_API_URL=http://localhost:8000
