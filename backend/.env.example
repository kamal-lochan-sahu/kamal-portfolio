# Copy this file to .env and fill in real values. Never commit .env itself.

# Required — Gemini AI for Ask Me / JD Matcher features
GEMINI_API_KEY=

# Optional but recommended — raises GitHub API rate limit from 60/hr to
# 5000/hr and gives accurate follower/repo stats. Create one at
# https://github.com/settings/tokens (no scopes needed for public data).
GITHUB_TOKEN=

# Optional — makes the Contact brief form deliver to your inbox. Without these
# the form falls back to opening the visitor's email app. For Gmail use an
# App Password (Google Account > Security > App passwords), not your login.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
CONTACT_TO=
